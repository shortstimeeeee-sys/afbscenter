# AFBS 센터 프로젝트 구조

## 📁 파일 구조

```
src/main/resources/static/
├── index.html                 # 대시보드
├── layout.html                # 공통 레이아웃 템플릿 (참고용)
│
├── bookings.html              # 예약/대관 관리
├── members.html               # 회원 관리
├── payments.html              # 결제/정산
├── facilities.html            # 시설/슬롯 설정
├── products.html              # 상품/이용권
├── attendance.html            # 출석/이용 기록
├── coaches.html               # 코치/레슨 관리
├── training-logs.html         # 훈련 기록/측정 데이터
├── analytics.html             # 통계/분석 리포트
├── announcements.html         # 공지/메시지
├── settings.html              # 설정 (관리자)
│
├── css/
│   ├── common.css             # 공통 스타일 (다크모드)
│   ├── dashboard.css          # 대시보드 전용
│   ├── bookings.css           # 예약 관리 전용
│   ├── members.css            # 회원 관리 전용
│   ├── payments.css           # 결제/정산 전용
│   ├── facilities.css         # 시설 관리 전용
│   ├── products.css           # 상품 관리 전용
│   ├── attendance.css         # 출석 관리 전용
│   ├── coaches.css            # 코치 관리 전용
│   ├── training-logs.css      # 훈련 기록 전용
│   ├── analytics.css          # 통계/분석 전용
│   ├── announcements.css      # 공지 전용
│   └── settings.css           # 설정 전용
│
└── js/
    ├── common.js              # 공통 JavaScript (API, 유틸리티, 권한 관리)
    ├── dashboard.js           # 대시보드 로직
    ├── bookings.js            # 예약 관리 로직
    ├── members.js             # 회원 관리 로직
    ├── payments.js            # 결제/정산 로직
    ├── facilities.js          # 시설 관리 로직
    ├── products.js            # 상품 관리 로직
    ├── attendance.js          # 출석 관리 로직
    ├── coaches.js             # 코치 관리 로직
    ├── training-logs.js       # 훈련 기록 로직
    ├── analytics.js           # 통계/분석 로직
    ├── announcements.js       # 공지 로직
    └── settings.js            # 설정 로직
```

## 🎨 디자인 시스템

### 다크모드 색상 팔레트
- **배경**: `#0F1117` (Primary), `#151924` (Secondary), `#1C2130` (Tertiary)
- **텍스트**: `#E6E8EB` (Primary), `#A1A6B3` (Secondary), `#6B7280` (Muted)
- **강조색**: `#5E6AD2` (Primary Accent)
- **상태색**: `#2ECC71` (Success), `#F1C40F` (Warning), `#E74C3C` (Danger)

### 공통 컴포넌트
- 사이드바 (260px 고정)
- 상단바 (검색, 알림, 사용자 메뉴)
- 카드 레이아웃
- 모달
- 테이블
- 버튼 (Primary, Secondary, Success, Danger)
- 배지 (Status 표시)
- KPI 카드

## 🔐 권한 구조

### Admin (관리자)
- 전체 접근
- 설정 관리
- 권한 관리

### Manager (매니저)
- 예약/회원/결제/운영/통계
- 시설 관리
- 상품 관리

### Coach (코치)
- 수업/출석/훈련 기록
- 선수 메모

### Front (데스크)
- 예약/결제/회원 기본
- 출석 처리

## 📋 페이지별 기능

### 1. 대시보드 (index.html)
- KPI 카드 (예약 수, 방문 수, 매출, 미수금)
- 빠른 작업 버튼
- 오늘 일정 타임라인
- 미처리 알림

### 2. 예약/대관 관리 (bookings.html)
- 달력 뷰 / 목록 뷰 전환
- 시설별 색상 구분
- 예약 상태 관리
- 대기 예약 승인/반려

### 3. 회원 관리 (members.html)
- 회원 목록 (등급, 상태 필터)
- 회원 상세 (이용권, 예약, 출석, 메모 탭)
- 회원 등록/수정/삭제

### 4. 결제/정산 (payments.html)
- 결제 내역 목록
- 결제 처리
- 환불 처리
- 정산 리포트

### 5. 시설/슬롯 설정 (facilities.html)
- 시설 CRUD
- 슬롯 관리 (요일/시간대별)
- 운영시간 설정

### 6. 상품/이용권 (products.html)
- 상품 CRUD
- 가격/할인 설정
- 유효기간 관리

### 7. 출석/이용 기록 (attendance.html)
- 오늘 예약 목록
- 체크인 처리
- 출석 기록 조회

### 8. 코치/레슨 관리 (coaches.html)
- 코치 관리
- 레슨 일정
- 레슨 기록

### 9. 훈련 기록 (training-logs.html)
- 타격 기록 (스윙수, 타구속도, 발사각, 컨택률)
- 투구 기록 (구속, 회전수, 구종, 스트라이크율)
- 체력 기록 (러닝, 컨디션)

### 10. 통계/분석 (analytics.html)
- 운영 지표 (가동률, 시간대별 수요, 취소율, 노쇼율)
- 매출 지표 (카테고리별, 추이)
- 회원 지표 (신규/이탈, 활동 회원)

### 11. 공지/메시지 (announcements.html)
- 공지 등록/관리
- 메시지 발송

### 12. 설정 (settings.html)
- 시스템 설정
- 권한 관리

## 🔌 API 엔드포인트 (필요한 백엔드 구현)

### 대시보드
- `GET /api/dashboard/kpi` - KPI 데이터
- `GET /api/dashboard/today-schedule` - 오늘 일정
- `GET /api/dashboard/alerts` - 미처리 알림

### 예약 관리
- `GET /api/bookings` - 예약 목록
- `GET /api/bookings/{id}` - 예약 상세
- `POST /api/bookings` - 예약 등록
- `PUT /api/bookings/{id}` - 예약 수정
- `DELETE /api/bookings/{id}` - 예약 삭제

### 회원 관리
- `GET /api/members` - 회원 목록
- `GET /api/members/{id}` - 회원 상세
- `POST /api/members` - 회원 등록
- `PUT /api/members/{id}` - 회원 수정
- `DELETE /api/members/{id}` - 회원 삭제
- `GET /api/members/{id}/products` - 회원 이용권
- `GET /api/members/{id}/bookings` - 회원 예약 내역
- `GET /api/members/{id}/attendance` - 회원 출석 내역

### 결제/정산
- `GET /api/payments` - 결제 목록
- `GET /api/payments/summary` - 정산 요약
- `POST /api/payments` - 결제 처리
- `POST /api/payments/{id}/refund` - 환불 처리

### 시설 관리
- `GET /api/facilities` - 시설 목록
- `GET /api/facilities/{id}` - 시설 상세
- `POST /api/facilities` - 시설 등록
- `PUT /api/facilities/{id}` - 시설 수정
- `DELETE /api/facilities/{id}` - 시설 삭제
- `GET /api/facilities/{id}/slots` - 슬롯 목록

### 상품 관리
- `GET /api/products` - 상품 목록
- `GET /api/products/{id}` - 상품 상세
- `POST /api/products` - 상품 등록
- `PUT /api/products/{id}` - 상품 수정
- `DELETE /api/products/{id}` - 상품 삭제

### 출석 관리
- `GET /api/attendance` - 출석 기록
- `POST /api/attendance/checkin` - 체크인 처리

### 코치 관리
- `GET /api/coaches` - 코치 목록
- `GET /api/coaches/{id}` - 코치 상세
- `POST /api/coaches` - 코치 등록
- `PUT /api/coaches/{id}` - 코치 수정
- `DELETE /api/coaches/{id}` - 코치 삭제
- `GET /api/lessons` - 레슨 목록

### 훈련 기록
- `GET /api/training-logs` - 기록 목록
- `GET /api/training-logs/{id}` - 기록 상세
- `POST /api/training-logs` - 기록 등록
- `PUT /api/training-logs/{id}` - 기록 수정
- `DELETE /api/training-logs/{id}` - 기록 삭제

### 통계/분석
- `GET /api/analytics` - 통계 데이터

### 공지/메시지
- `GET /api/announcements` - 공지 목록
- `POST /api/announcements` - 공지 등록
- `DELETE /api/announcements/{id}` - 공지 삭제
- `GET /api/messages` - 메시지 목록
- `POST /api/messages` - 메시지 발송

### 설정
- `GET /api/settings` - 설정 조회
- `PUT /api/settings` - 설정 저장
- `GET /api/users` - 사용자 목록

## 🚀 다음 단계

1. **백엔드 API 구현**
   - 위에 나열된 모든 API 엔드포인트 구현
   - 데이터 모델 확장 (Booking, Facility, Product, Payment, Attendance, Coach, TrainingLog 등)

2. **데이터베이스 스키마 확장**
   - 예약 테이블
   - 시설 테이블
   - 상품 테이블
   - 결제 테이블
   - 출석 테이블
   - 코치 테이블
   - 훈련 기록 테이블
   - 공지 테이블

3. **차트 라이브러리 연동** (선택)
   - Chart.js 또는 다른 차트 라이브러리
   - 통계/분석 페이지의 차트 구현

4. **권한 시스템 구현**
   - 사용자 인증/인가
   - 역할 기반 접근 제어
